package Exam.Q1;

import javax.swing.*;

public class b {
    /**
     * if Max < min then
     * return false
     * else
     * misPos = (min + max) / 2
     * midValue = L.get(midPos)
     *   if N = MidValue then
     *   else if N < MidValue the
     *   retun BinSearch
     *   else
     *   return binSearch
     */
}
