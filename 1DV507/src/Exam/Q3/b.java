package Exam.Q3;

public class b {
    /** explanation and code
     * map / binarySearch / ..... in other cases
     *
     * maps is a set of key / value pairs. the two keo operations are put and get
     * V put (k key, v value) // Associate the specified value with the specified key
     * V get (k key) // the value to which the specified key is mapped
     *
     * usage:
     * Map <Integer, Stirng> map = new Hashmap<Integer, String>();
     * map.put(64, "C64");
     *
     * System.out.println
     */
}
